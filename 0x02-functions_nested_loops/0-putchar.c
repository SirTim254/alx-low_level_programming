#include "main.h"

/*
 * main:start of the programme
 * Main -Start of programme
 * Return: Return zero on success
 */

int main(void)
{
		char *sh = "_putchar";
			while (*sh)
			{
			_putchar(*sh);
			sh++;										}
			_putchar('\n');
			return (0);
}
